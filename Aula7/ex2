T = [11,7,2,4]

menor = 999999

for i in range(len(T)):
    if T[i] < menor:
        menor = T[i]
print("Menor valor da lista: %d "  %menor)

#menor = -999999

#for i in range(len(T)):
    #if T[i] < menor:
        #menor = T[i]
#print("Menor valor da lista: %d "  %menor)